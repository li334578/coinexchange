spring:
  application:  # 应用名称
    name: admin-service
  profiles:  #切换到开发环境上面
    active: dev
  cloud:
    nacos:  #配置Nacos的地址
      server-addr: 39.99.162.169:8849
      config:  # 配置文件的扩展名
        file-extension: yaml
# 拉取的配置文件的ID dataID = admin-service-dev.yaml